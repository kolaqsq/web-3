import { FilterWorkersPipe } from './filter-workers.pipe';

describe('FilterWorkersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWorkersPipe();
    expect(pipe).toBeTruthy();
  });
});
